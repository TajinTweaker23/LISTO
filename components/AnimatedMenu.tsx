
import React from 'react';
import '../styles/AnimatedMenu.css';

const AnimatedMenu: React.FC = () => {
  return (
    <div className="animated-menu">
      {/* Animated menu component - temporarily disabled for GSAP 3.x compatibility */}
      <p>Menu component placeholder</p>
    </div>
  );
};

export default AnimatedMenu;
